# Codex implementation integration

Reserved for concrete Codex/runtime dispatch integration. The provider-neutral implementation evidence contract lives in `src/gates/implementation-contract.js` so application orchestration does not depend on a concrete Codex adapter.
