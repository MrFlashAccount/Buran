# JSON registry storage integration

Owns concrete JSON files, path layout, atomic writes, event journals, indexes, and snapshots. It must preserve the durable registry format exposed by the execution-runs context.
