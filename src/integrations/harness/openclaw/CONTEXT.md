# OpenClaw harness adapter

Concrete OpenClaw integration lives here, behind the provider-neutral HarnessRuntime port. It must not mutate Buran registry files directly and must return only bounded status/evidence summaries.
