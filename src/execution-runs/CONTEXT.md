# Execution runs context

Owns durable run state language: schema, state machine, recovery policy, and registry API contract. Concrete JSON/filesystem persistence lives under `integrations/storage/json-registry/`.
